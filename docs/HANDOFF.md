# ExperimentAdvisor 交接

```yaml
slice_status: in_progress
current_slice: execute_and_backfill_pichia_hlf_round2
next_action: obtain_reviewed_round2_measurements
```

## 当前目标

Round 1 已用真实数据（菌株 Y103，16 个条件）完成回填与分析。基于这批数据生成的真实 Round 2 设计（响应面 CCD 18 点 + 补料间隔交互 2 点 + 噪声参考 2 点 + LHS 10 点，共 32 个条件）已经就位，等待摇瓶实验实际执行、产量与 OD600 回填。**负责搭建这套 Round 1/2 分析流程的同事已离职**，回填后的分析（CCD 响应面拟合、补料间隔交互检验、合并数据贝叶斯优化）需要接手人自己核对结果是否合理——这些分析逻辑用合成数据验证过正确性，但从未跑过真实 Round 2 数据。

## 下一步

1. 在 Pichia hLF 页面的 Round 2 页签，用"生成完整 Round 2 设计"拿到 32 个条件的设计表（Excel 下载），确认已经排期/执行的批次和这份设计一致。
2. 摇瓶实验执行后，记录每个条件的产量与 OD600，通过同一页签的"上传已回填结果"传回。
3. 检查"Round 2 结果分析"部分：CCD 响应面的 R²/失拟检验是否合理、补料间隔交互检验的结论、以及要不要用"合并数据后的贝叶斯优化建议"生成 Round 3 候选——这几块逻辑见 `docs/adr/0009` 到 `0013`。
4. 响应面的可视化（等高线图、系数表、残差图）、以及点估计之外的四项深入分析（置信区间、灵敏度、鞍点/岭线判别、产量+OD600联合满意度，其中灵敏度/鞍点/联合满意度三项现在也各配了一张图，见 `docs/adr/0014`）都已经接到界面上并通过合成数据验证；真实数据回填后重点核对这四项深入分析在四种边界情况（失拟显著/最优点卡边界/OD600不可行/一切正常）下给出的文案和真实数据是否对得上。
5. 贝叶斯优化只剩一个入口——"合并数据后的贝叶斯优化建议"（Round1+Round2 合并数据训练，见 `docs/adr/0009`）；原来还有一个"仅用 Round1 数据"的预览入口和依赖它的"历史记录"页签，2026-08-12 已确认删除（见 `docs/adr/0016`，因为 Round 2 的实际设计早就照 CCD 方法论定下来了，那个预览入口的建议从没被采纳过）。它的"模型校验（留一法交叉验证）"折叠框需要手动点按钮才会跑（几秒到几十秒），见 `docs/adr/0015`；仅用 Round 1 真实数据跑出来的产量模型 Q²≈0.13，明显偏弱——数据量增加后应该重新点一次这个按钮，确认 Q² 有没有随数据变多而改善，再决定贝叶斯优化推荐能不能直接采用。
6. **待实施（已和用户对齐方向，2026-08-12，下一轮开始）**：Round 2 页签内容太长（`_pichia_round2_tab`+`_pichia_round2_results_analysis_section` 一路往下滚），`App/pages_pichia.py` 单文件 2482 行、Round 2 相关代码占了一半以上——计划做两件事：
   - **UI 分页**：在 Round 2 tab 内部再加一层 `st.tabs`，按现有内容拆成 4 个子页——显著性分析（噪声/活跃变量筛选+联合探索可加性检查）、设计生成与回填（CCD设计表+下载/上传/回填）、响应面结果（CCD拟合+图表+深入分析+补料间隔交互检验）、合并数据贝叶斯优化。延续现有"外层 Round1/Round2 tabs"的模式，不引入新导航框架。
   - **文件拆分**：`pages_pichia.py` 按职责切开——共享工具（常量/格式化/设计表通用逻辑）、Round1（构建器+图表+上传解析）、Round2 图表与解读（响应面/灵敏度/鞍点/满意度/BO交叉验证的图表表格构建函数，目前最占体量）、Round2 编排（把图表拼成小节的那几个大函数）；`pages_pichia.py` 最后只剩顶层 tab 路由。
   - 开始前先写一份具体的文件划分+每个函数归属清单给用户确认，再动手（用户明确要求先出计划）。

## 必读材料

1. [需求](REQUIREMENTS.md)：目标、验收与不可作出的声明。
2. [架构](ARCHITECTURE.md)：活跃/历史路径和数据边界。
3. `docs/adr/0009` 至 `0016`：这轮 Round 2 方法论决策（为什么选 CCD、为什么不再要求每点测 2 瓶、技术噪声为什么不并入阈值、新补料间隔为什么没写进共享常量、响应面拟合的具体做法、点估计之外四项深入分析的设计取舍、贝叶斯优化的留一法校验和叙述性解读、为什么删除 Round1-only 预览入口和历史记录页签）。
4. `tests/test_round1_design.py`、`tests/test_round2_design.py`、`tests/test_pages_pichia.py`：设计和分析的可执行约束。

## 验证方式

运行 `python -m pytest -q`；如改动 Streamlit 页面，再用 `streamlit run App/app.py` 检查默认 Pichia 页面、Round 1/2 导出回填与结果分析部分。这个开发环境里 Browser 预览面板经常无法渲染画面，验证 UI 改动时改用 `streamlit.testing.v1.AppTest` 驱动真实按钮点击（本次会话的脚本留在各自开发环境的临时目录，未提交到仓库）。

## 硬约束

- 当前项目是从零开始的毕赤酵母 hLF 摇瓶实验；没有可用的 hLF 历史数据。
- HMO/2FL 发酵罐数据已确认无效，相关代码和归档只能作为历史参考，不能用于当前实际推荐。
- 原始、处理后和用户上传的发酵数据不得提交到版本库。
- 软件建议必须由研发人员审阅，并与工艺团队确认后才可执行；软件不自动授权实验。
