# ADR-0002：发酵数据不进入版本控制

## 背景

原始、处理后及用户上传的发酵数据可能包含敏感实验信息，且会随每轮实验变化。

## 决策

通过 `.gitignore` 将 `data/` 默认排除，只保留毕赤酵母目录结构所需的模板和空目录标记。生成的敏感报告也保持忽略。

## 后果

可复现性依赖公开的代码、模板和测试；实际数据需要由拥有者在受控位置保存和交接。任何改变该数据边界的提议必须先新建 ADR。

## 取代关系

无。
